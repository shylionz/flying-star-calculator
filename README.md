# Flying Star Static UI

Run locally from the project root:

```bash
cd ~/sandbox/flying-star-calculator/ui
python3 -m http.server 8765
```

Open:

```text
http://localhost:8765/
```

This is a static browser prototype. It does not modify the Python calculation engine, lookup tables, or validation corpus.
